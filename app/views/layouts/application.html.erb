<!DOCTYPE html>
<html>
  <head>
    <title>Company</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= render :partial => "shared/menu" %>
    <!-- <h1><%#= I18n.t('welcome') %></h1> -->
    <!-- <h1><%#= t('welcome', user: "John Doe") %></h1> -->
    <%# t es de translate %>

    <div class="container" style="margin-top: 80px !important; min-height: 950px;">
      <!-- Espacio para mostrar los mensajes de aplicación (:flash, :notice, :error, etc)" -->
      <% flash.each do |name, msg| %>
        <%= content_tag :div, msg, class: "alert alert-info" %>
      <% end %>

    <% if user_signed_in? %>
      <h1><%= t('welcome', employee: current_user.first_name) %></h1>
    <% end %>

    <code>
      User Signed IN?: <%= user_signed_in? %><br>
      Current User?: <%= current_user %><br>
      Can Manage ALL? <%= can? :manage, :all %><br>
      <!-- quiere decir: este usuario loguedo, ¿tiene acceso a todo? -->
      Can Toggle Administrators? <%= can? :toggle_admin, :administrator %><br>
    </code>

    <div style="position: fixed; bottom: 0; right: 0;">
      <%= render 'shared/language_switcher' %>
    </div>

    <%= yield %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js" integrity="sha384-JEW9xMcG8R+pH31jmWH6WWP0WintQrMb4s7ZOdauHnUtxwoG2vI5DkLtS3qm9Ekf" crossorigin="anonymous"></script>
  </body>
</html>
